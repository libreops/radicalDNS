image: alpine

stages:
  - test

test:
  stage: test
  script:
    - apk add ansible-lint shellcheck shfmt
    - ansible-lint main.yml
    - shfmt -f . | xargs shellcheck -x
    - shfmt -i 2 -ci -d .
